# vault-sync reads runtime configuration exclusively from the Web UI
# (stored encrypted in /data/config.enc). Container env vars (all optional):

PORT=8770                     # listen port
KEY_FILE=/run/secrets/key     # encryption key mount (see README security model)
DATA_DIR=/data                # state: config.enc, sync.log, backups/
TZ=Asia/Hong_Kong             # schedule timezone
